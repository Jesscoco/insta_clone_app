<p id="notice"><%= notice %></p>
<p>
  <strong>Image:</strong>
  <% if @post.image && @post.image.url %>
  <%= image_tag @post.image.url , height: "250px" %>
  <% end %>
</p>
<%= link_to 'Edit', edit_post_path(@post) %> 
<%= link_to 'Back', posts_path %>
